{
  "name": "QMMS Trading System",
  "version": "2.2.4",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm ci && npm run build",
    "build:installer": "cd installer && npm ci && npm run build",
    "dist": "npm run build:frontend && npm run build:installer"
  },
  "dependencies": {
    "lucide-react": "^0.255.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-builder": "^23.6.0"
  }
}
