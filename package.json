{
  "name": "qmms-monorepo",
  "version": "2.2.6",
  "private": true,
  "workspaces": [
    "frontend",
    "installer",
    "."
  ],
  "main": "installer/main.js",
  "scripts": {
    "bootstrap": "npm ci && npm run build --workspace frontend && npm run build --workspace installer"
  }
}

